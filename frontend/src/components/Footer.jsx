import React from 'react';

const footerStyle = {
  position: 'absolute',
  bottom: 0,
  width: '100%',
  backgroundColor: '#2a2c32',
  color: 'white',
  textAlign: 'center',
};

const Footer = () => {
  return (
    <div style={footerStyle}>
      123
    </div>
    );
}

export default Footer;
